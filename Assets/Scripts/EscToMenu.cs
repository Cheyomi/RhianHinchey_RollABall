using UnityEngine;
using UnityEngine.SceneManagement;

public class EscToMenu : MonoBehaviour
{
    bool paused = false;

    void Update()
    {
        if (paused == true)
        {

        }
    }
}
